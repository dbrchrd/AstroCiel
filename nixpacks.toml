[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]
cacheDirectories = ["node_modules/.cache"]

[start]
cmd = "bun ./dist/server/entry.mjs"
